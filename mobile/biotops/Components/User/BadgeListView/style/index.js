'use strict';
import {StyleSheet} from 'react-native';

const BADGE_VIEW_STYLE = StyleSheet.create({

    badgeContainer: {
        flexDirection: 'row',
        flexWrap: 'wrap',
    },

    badgeModal: {
        alignItems: 'center',
        justifyContent: 'center',
    },
});

export default BADGE_VIEW_STYLE;